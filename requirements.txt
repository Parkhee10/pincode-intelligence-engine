fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1

pytest==8.2.2
httpx==0.27.0

# added in later stages, kept here so the file evolves with real history:
# pandas, scikit-learn, xgboost — Stage 1 (data pipeline + model)
